<form>
  {{#if product.hasChildren}}
    <p>组合产品</p>
  {{/if}}
  <p>种类：{{ product.group }}</p>
  <p>名称: {{ product.name }}</p>
  <p>价格: ￥{{ product.price }}</p>
  <button type="submit">购买</button>
</form>
